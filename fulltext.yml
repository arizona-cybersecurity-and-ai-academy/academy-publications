# Manual canonical-link overrides, for records with no DOI and no usable URL. Key = DOI (lower-case) or Zotero item key; value = the publisher-of-record URL.
# No third-party resolvers are used anywhere in this build; every link on the page is the canonical one plus its UA-proxied twin.
